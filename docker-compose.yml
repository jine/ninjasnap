version: '3.8'

services:
  ninjasnap:
    build: .
    container_name: ninjasnap-app
    ports:
      - '42069:3000'
    volumes:
      - ./public/screenshots:/app/public/screenshots
      - ./logs:/app/logs
    # For production, use:
    # command: ["npm", "start"]
