version: '3.8'

services:
  ninjasnap:
    build: .
    container_name: ninjasnap-app
    ports:
      - "3000:3000"
    volumes:
      - ./public/screenshots:/app/public/screenshots
    # For production, use:
    # command: ["npm", "start"]